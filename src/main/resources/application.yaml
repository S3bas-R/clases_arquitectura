spring:
  application:
    name: students

  datasource:
    url: jdbc:h2:mem:studentsdb
    driver-class-name: org.h2.Driver
    username: admin
    password: admin

  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      del-auto: update

server:
  port:8787